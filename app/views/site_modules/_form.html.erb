<%= form_with(model: site_module, local: true) do |form| %>
  <% if site_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(site_module.errors.count, "error") %> prohibited this site_module from being saved:</h2>

      <ul>
        <% site_module.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
